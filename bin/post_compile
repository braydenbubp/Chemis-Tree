#!/usr/bin/env bash
set -eo pipefail

echo "-----> Post-compile hook"
echo "-----> Configuring RDKit"

# Create RDKit system configuration
if [ ! -f $HOME/.rdkit ]; then
    echo "Creating RDKit system configuration"
    mkdir -p $HOME/.rdkit
fi

echo "-----> Post-compile hook done"
